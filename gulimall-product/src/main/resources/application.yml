#商品服务
server:
  port: 10000

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://60.204.249.234:3306/gulimall_pms
    driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 60.204.249.234:8848
  application:
    name: gulimall-product

# MapperScan
# sql映射文件位置
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
